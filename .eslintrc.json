{
  "extends": [
    "react-app",
    "react-app/jest"
  ],
  "plugins": [
    // Remove "unused-imports" if you don't need it
  ],
  "rules": {
    // Remove any unused-imports rules if present
  }
}
